[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "creatorcut"
version = "0.1.0"
description = "Explainable multimodal ranking for creator video clips"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
  "faster-whisper==1.2.1",
]

[project.optional-dependencies]
dev = [
  "pytest>=8,<10",
  "ruff>=0.11,<1",
]

[project.scripts]
creatorcut-validate = "creatorcut.dataset:main"
creatorcut-transcribe = "creatorcut.transcription:main"
creatorcut-build-dataset = "creatorcut.dataset:build_dataset_main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
